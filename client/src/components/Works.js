import React from 'react'
import Work from 'components/Work'

function Works(props) {
  /*let worksList = <Work props={props} />.map(work => (
    <ul className='projects-list'>{worksList}</ul>
  ))
  */
  return null
}

export default Works
